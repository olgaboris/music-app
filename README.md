The application for managing music

# How to start app

In terminal run:

* `npm i` - install npm dependencies,
* `npm start` - start bmb.dev server on [http://localhost:3000/](http://localhost:3000/),